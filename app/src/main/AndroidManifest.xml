<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.varmin.vdemo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".others.MultipleStatusActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".MainActivity" /> -->
        <!-- <activity android:name=".henCoder.CustomViewActivity"></activity> -->
        <!-- <activity android:name=".others.LoadBigBitmapActivity"></activity> -->
        <!-- <activity android:name=".others.OkHttpTestActivity"></activity> -->
        <!-- <activity android:name=".henCoder.ScalableImageActivity"></activity> -->
        <!-- <activity android:name=".test.TestActivity"></activity> -->
        <!-- <activity android:name=".henCoder.MutilPointActivity"></activity> -->
        <!-- <activity android:name=".others.RxJava2TestActivity"></activity> -->
        <!--<activity android:name=".others.MultipleStatusActivity"></activity>-->
    </application>

</manifest>